import { useEffect, useState, type FormEvent } from 'react';
import { Landmark, Plus } from 'lucide-react';
import { useTranslation } from 'react-i18next';
import { AppLayout } from '@/components/layout/AppLayout';
import { apiGet, apiRequest } from '@/lib/api-client';
type Account={id:string;name:string;account_type:string;currency:string;balance:string};
export default function Page(){const{i18n}=useTranslation();const ar=i18n.language.startsWith('ar');const[rows,setRows]=useState<Account[]>([]);const[show,setShow]=useState(false);const[error,setError]=useState('');const load=()=>apiGet<Account[]>('/finance/accounts').then(setRows).catch((e:Error)=>setError(e.message));useEffect(()=>{void load();},[]);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await apiRequest('/finance/accounts',{method:'POST',body:JSON.stringify({name:f.get('name'),type:f.get('type'),currency:'EGP',openingBalance:Number(f.get('balance')).toFixed(2)})});setShow(false);await load();}catch(c){setError(c instanceof Error?c.message:'Failed');}};return <AppLayout><section className="mb-6 flex justify-between"><div><div className="mb-2 flex gap-2 text-primary-600"><Landmark className="h-5 w-5"/>{ar?'المالية':'Finance'}</div><h1 className="text-2xl font-bold">{ar?'النقدية والبنوك':'Cash and banks'}</h1></div><button onClick={()=>setShow(true)} className="btn btn-primary btn-sm"><Plus className="h-4 w-4"/>{ar?'حساب جديد':'New account'}</button></section>{error&&<div className="mb-4 rounded-xl bg-danger-50 p-4 text-danger-700">{error}</div>}<section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rows.map(r=><div className="stat-card" key={r.id}><p className="stat-label">{r.name} · {r.account_type}</p><p className="mt-3 text-2xl font-bold">{Number(r.balance).toLocaleString()} {r.currency}</p></div>)}</section>{show&&<div className="fixed inset-0 z-50 grid place-items-center bg-navy-950/60 p-4" role="dialog" aria-modal="true" aria-labelledby="new-account-title"><form onSubmit={submit} className="card w-full max-w-md p-5"><h2 id="new-account-title" className="mb-4 text-lg font-bold">{ar?'حساب مالي جديد':'New financial account'}</h2><input name="name" required minLength={2} className="input mb-3" placeholder={ar?'اسم الحساب':'Account name'}/><select name="type" className="select mb-3"><option value="cash">Cash</option><option value="bank">Bank</option><option value="wallet">Wallet</option></select><input name="balance" type="number" step="0.01" defaultValue="0" className="input"/><div className="mt-4 flex justify-end gap-2"><button type="button" onClick={()=>setShow(false)} className="btn btn-secondary">{ar?'إلغاء':'Cancel'}</button><button className="btn btn-primary">{ar?'حفظ':'Save'}</button></div></form></div>}</AppLayout>}
